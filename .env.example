# Proxy Configuration
# These are set automatically by the dev:proxy script
# Uncomment to customize:

# HTTP_PROXY=http://127.0.0.1:8080

# Required when using HTTPS proxy with self-signed certificate
# NODE_TLS_REJECT_UNAUTHORIZED=0
